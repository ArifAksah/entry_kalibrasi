services:
  pdf-template-service:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./templates:/app/templates
      - ./cache:/app/cache
    env_file:
      - .env
    restart: unless-stopped
